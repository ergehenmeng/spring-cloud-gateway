spring:
  application:
    name: gateway
  profiles:
    active: nacos
  cloud:
    nacos:
      password: nacos
      username: nacos
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
    stream:
      rabbit:
        bindings:
      binders:
        xxx


  rabbitmq:
    host:
